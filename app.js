const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const money=n=>new Intl.NumberFormat('es-CO',{style:'currency',currency:'COP',maximumFractionDigits:0}).format(Number(n)||0);
const today=()=>new Date().toISOString().slice(0,10);
function isoWeek(d=new Date()){const x=new Date(Date.UTC(d.getFullYear(),d.getMonth(),d.getDate()));x.setUTCDate(x.getUTCDate()+4-(x.getUTCDay()||7));const y=new Date(Date.UTC(x.getUTCFullYear(),0,1));return `${x.getUTCFullYear()}-W${String(Math.ceil((((x-y)/86400000)+1)/7)).padStart(2,'0')}`}
let state={user:null,week:isoWeek(),works:[],payments:[],closes:[],audit:[],closed:false};
const demo={users:{karen:{password:'demo',role:'admin',name:'Karen'},taller:{password:'demo',role:'operador',name:'Encargada del taller'}}};
const sha=async t=>[...new Uint8Array(await crypto.subtle.digest('SHA-256',new TextEncoder().encode(t)))].map(x=>x.toString(16).padStart(2,'0')).join('');
function jsonp(payload){return new Promise((resolve,reject)=>{const cb='tallerCb'+Date.now()+Math.random().toString(16).slice(2),s=document.createElement('script'),timer=setTimeout(()=>done(null,Error('El servidor tardó demasiado')),15000);function done(v,e){clearTimeout(timer);delete window[cb];s.remove();e?reject(e):resolve(v)}window[cb]=r=>r.ok?done(r.data):done(null,Error(r.error||'No fue posible completar la operación'));s.onerror=()=>done(null,Error('No fue posible conectar con el servidor'));s.src=window.APP_CONFIG.API_URL+'?callback='+cb+'&payload='+encodeURIComponent(JSON.stringify(payload));document.head.appendChild(s)})}
async function loginReal(data){const c=await jsonp({action:'challenge',username:data.username}),verifier=await sha(c.salt+data.password),proof=await sha(verifier+c.nonce),out=await jsonp({action:'login',username:data.username,nonce:c.nonce,proof});localStorage.tallerToken=out.token;return out.user}
async function api(action,data={}){if(window.APP_CONFIG.DEMO_MODE){await new Promise(r=>setTimeout(r,120));return demoApi(action,data)}if(action==='login'){try{return await loginReal(data)}catch(e){if(!String(e.message).toLowerCase().includes('vencida'))throw e;return loginReal(data)}}return jsonp({action,token:localStorage.tallerToken,...data})}
function demoApi(a,d){const db=JSON.parse(localStorage.tallerDemo||'{"works":[],"payments":[],"closes":[],"audit":[]}');const save=()=>localStorage.tallerDemo=JSON.stringify(db);if(a==='login'){const u=demo.users[d.username];if(!u||u.password!==d.password)throw Error('Usuario o contraseña incorrectos');localStorage.tallerToken=d.username;return u}if(a==='logout'){localStorage.removeItem('tallerToken');return true}if(a==='session'){const u=demo.users[localStorage.tallerToken];if(!u)throw Error('Sesión vencida');return u}if(a==='list')return {...db,closed:db.closes.some(x=>x.week===d.week),works:db.works.filter(x=>x.week===d.week),payments:db.payments.filter(x=>x.week===d.week)};const u=demo.users[localStorage.tallerToken];if(!u)throw Error('Sin autorización');if(a==='saveWork'){if(db.closes.some(x=>x.week===d.item.week))throw Error('La semana está cerrada');db.works.push({...d.item,id:crypto.randomUUID(),createdAt:new Date().toISOString(),createdBy:u.name});db.audit.unshift({at:new Date().toISOString(),user:u.name,event:'Registró trabajo',detail:d.item.clienta});save();return true}if(a==='savePayment'){if(u.role!=='admin')throw Error('Solo Karen registra abonos');if(db.closes.some(x=>x.week===d.item.week))throw Error('La semana está cerrada');db.payments.push({...d.item,id:crypto.randomUUID(),createdAt:new Date().toISOString(),createdBy:u.name});db.audit.unshift({at:new Date().toISOString(),user:u.name,event:'Registró abono',detail:money(d.item.valor)});save();return true}if(a==='closeWeek'){if(u.role!=='admin')throw Error('Solo Karen puede cerrar');if(db.closes.some(x=>x.week===d.week))throw Error('Ya está cerrada');const works=db.works.filter(x=>x.week===d.week),pays=db.payments.filter(x=>x.week===d.week);db.closes.unshift({week:d.week,count:works.length,produced:works.reduce((s,x)=>s+Number(x.valor),0),paid:pays.reduce((s,x)=>s+Number(x.valor),0),closedAt:new Date().toISOString(),closedBy:u.name});db.audit.unshift({at:new Date().toISOString(),user:u.name,event:'Cerró semana',detail:d.week});save();return true}throw Error('Acción no disponible')}
function toast(t){$('#toast').textContent=t;$('#toast').classList.add('show');setTimeout(()=>$('#toast').classList.remove('show'),2200)}
async function load(){const d=await api('list',{week:state.week});Object.assign(state,d);render()}
function render(){const produced=state.works.reduce((s,x)=>s+Number(x.valor),0),paid=state.payments.reduce((s,x)=>s+Number(x.valor),0),bal=produced-paid;$('#weekTitle').textContent=state.week.replace('-W',' · Semana ');$('#mCount').textContent=state.works.length;$('#mProduced').textContent=money(produced);$('#mPaid').textContent=money(paid);$('#balanceLabel').textContent=bal>=0?'Saldo pendiente':'Saldo a favor de Karen';$('#mBalance').textContent=money(Math.abs(bal));$('#weekStatus').textContent=state.closed?'Cerrada · sin modificaciones':'Abierta';const statuses=['Pendiente','En proceso','Terminado','Entregado'];$('#statusBars').innerHTML=statuses.map(s=>{const n=state.works.filter(x=>x.estado===s).length,p=state.works.length?Math.round(n/state.works.length*100):0;return `<div class="bar"><span>${s}</span><div class="track"><div class="fill" style="width:${p}%"></div></div><b>${n}</b></div>`}).join('');$('#workList').innerHTML=state.works.length?state.works.map(x=>`<article class="row"><div><b>${esc(x.clienta)}</b><p>${esc(x.prenda)} · ${esc(x.tipo)}</p><span class="badge">${esc(x.estado)}</span><p class="muted">Registró: ${esc(x.createdBy)}</p></div><span class="money">${money(x.valor)}</span></article>`).join(''):'<div class="empty">No hay trabajos registrados esta semana.</div>';$('#paymentList').innerHTML=state.payments.length?state.payments.map(x=>`<article class="row"><div><b>${x.fecha} · ${esc(x.medio)}</b><p>${esc(x.nota||'Sin nota')}</p><p class="muted">Registró: ${esc(x.createdBy)}</p></div><span class="money">${money(x.valor)}</span></article>`).join(''):'<div class="empty">No hay abonos registrados esta semana.</div>';$('#closeList').innerHTML=state.closes.length?state.closes.map(x=>`<article class="row"><div><b>${x.week}</b><p>${x.count} prendas · Cerró ${esc(x.closedBy)}</p><p class="muted">${new Date(x.closedAt).toLocaleString('es-CO')}</p></div><div><div class="money">${money(x.produced-x.paid)}</div><small>saldo al cierre</small></div></article>`).join(''):'<div class="empty">Aún no hay cierres.</div>';$('#auditList').innerHTML=state.audit.map(x=>`<article class="row"><div><b>${esc(x.event)}</b><p>${esc(x.user)} · ${esc(x.detail)}</p></div><small>${new Date(x.at).toLocaleString('es-CO')}</small></article>`).join('')||'<div class="empty">Sin actividad.</div>';$$('[data-open]').forEach(b=>b.disabled=state.closed);$('#closeWeek').disabled=state.closed}
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function enter(u){state.user=u;$('#login').hidden=true;$('#app').hidden=false;$('#userBadge').textContent=`${u.name} · ${u.role==='admin'?'Administradora':'Taller'}`;$$('[data-admin]').forEach(x=>x.hidden=u.role!=='admin');load()}
$('#loginForm').addEventListener('submit',async e=>{e.preventDefault();const b=e.submitter;b.disabled=true;b.textContent='Ingresando…';$('#loginError').textContent='';try{enter(await api('login',{username:$('#username').value.trim().toLowerCase(),password:$('#password').value}))}catch(x){$('#loginError').textContent=x.message}finally{b.disabled=false;b.textContent='Iniciar sesión'}});$('#logoutBtn').onclick=async()=>{await api('logout');location.reload()};$$('nav button').forEach(b=>b.onclick=()=>{$$('nav button,.view').forEach(x=>x.classList.remove('active'));b.classList.add('active');$('#'+b.dataset.view).classList.add('active')});$$('[data-open="work"]').forEach(b=>b.onclick=()=>$('#workDialog').showModal());$$('[data-open="payment"]').forEach(b=>b.onclick=()=>{$('#paymentForm [name=fecha]').value=today();$('#paymentDialog').showModal()});$$('[data-close]').forEach(b=>b.onclick=()=>$('#'+b.dataset.close).close());$('#weekPicker').value=state.week;$('#weekPicker').onchange=e=>{state.week=e.target.value;load()};$('#saveWorkBtn').onclick=async()=>{const f=$('#workForm');if(!f.reportValidity())return;const item=Object.fromEntries(new FormData(f));item.week=state.week;await api('saveWork',{item});f.reset();$('#workDialog').close();toast('Trabajo registrado');load()};$('#savePaymentBtn').onclick=async()=>{const f=$('#paymentForm');if(!f.reportValidity())return;const item=Object.fromEntries(new FormData(f));item.week=state.week;await api('savePayment',{item});f.reset();$('#paymentDialog').close();toast('Abono registrado');load()};$('#closeWeek').onclick=async()=>{if(!confirm(`¿Cerrar ${state.week}? Después no se podrá modificar.`))return;await api('closeWeek',{week:state.week});toast('Semana cerrada');load()};(async()=>{try{enter(await api('session'))}catch{}})();
